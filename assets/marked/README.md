# marked.js

`marked.min.js` is downloaded here automatically by `setup.sh` during install:

```
https://cdn.jsdelivr.net/npm/marked@12.0.0/marked.min.js
```

Do **not** commit this file to the repo — it is excluded by `.gitignore`.
The `setup.sh` script fetches it fresh on every install.
